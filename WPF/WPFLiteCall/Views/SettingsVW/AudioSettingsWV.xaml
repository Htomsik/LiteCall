<UserControl
    d:DataContext="{d:DesignInstance {x:Type Vmds:AudioSettingsVmd},
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="300"
    d:DesignWidth="700"
    mc:Ignorable="d"
    x:Class="LiteCall.Views.SettingsVW.AudioSettingsWV"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:Vmds="clr-namespace:Core.VMD.AdditionalVmds.SettingsVmds;assembly=Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style TargetType="ComboBox" x:Key="SettingsComboBoxStyle"
               BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Width" Value="450" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBlock" 
               x:Key="SettingsHeaderStyle"
               BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Opacity" Value="0.9" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>
    </UserControl.Resources>

    <!--  Audio management  -->
    <Grid
        HorizontalAlignment="Stretch"
        MinWidth="300"
        VerticalAlignment="Top">

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!--  Input device selection  -->
        <Grid  Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" /> 
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Inpu device" 
                       Style="{StaticResource SettingsHeaderStyle}" />
            
            <ComboBox 
                Grid.Column="1"
                Style="{StaticResource SettingsComboBoxStyle}"
                ItemsSource="{Binding InputDevices}"
                SelectedItem="{Binding CaptureDevice}"
                DisplayMemberPath="Name"
            />
        </Grid>
        
        <!--  Output device selection  -->
        <Grid HorizontalAlignment="Stretch" Grid.Row="1" Margin="5" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" /> 
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Output device" 
                       Style="{StaticResource SettingsHeaderStyle}" />
            
            <ComboBox 
                Grid.Column="1"
                Style="{StaticResource SettingsComboBoxStyle}"
                ItemsSource="{Binding OutputDevices}"
                SelectedItem="{Binding OutputDevice}"
                DisplayMemberPath="Name"
                />
        </Grid>
        
    </Grid>

</UserControl>
